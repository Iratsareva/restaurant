# ⚡ БЫСТРАЯ ПРОВЕРКА РАБОТЫ ПРИЛОЖЕНИЯ

## 🚀 Запуск
```bash
docker compose up -d --build
```

## 🌐 Веб-интерфейсы (открыть в браузере)

| Сервис | URL | Логин/Пароль | Что проверить |
|--------|-----|--------------|---------------|
| **Demo REST API** | http://localhost:8081 | - | JSON с HATEOAS ссылками |
| **RabbitMQ** | http://localhost:15672 | guest/guest | Queues, Exchanges |
| **Prometheus** | http://localhost:9091 | - | Status → Targets (все UP) |
| **Grafana** | http://localhost:3000 | admin/admin | Dashboards с метриками |
| **Zipkin** | http://localhost:9411 | - | Трассировки запросов |
| **Jenkins** | http://localhost:8088 | admin/admin | Pipeline статус |

## 🔌 Быстрые API тесты (curl/Postman)

### Health Checks (все должны вернуть `{"status":"UP"}`)
```bash
curl http://localhost:8081/actuator/health    # Demo
curl http://localhost:9092/actuator/health    # Reservation Price
curl http://localhost:8082/actuator/health    # Audit
curl http://localhost:8083/actuator/health    # Notification
```

### Основные API endpoints
```bash
# Получить все столы
curl http://localhost:8081/api/tables

# Создать клиента
curl -X POST http://localhost:8081/api/clients \
  -H "Content-Type: application/json" \
  -d '{"name":"Тест","email":"test@test.com","phone":"123"}'

# Создать бронирование (проверит весь стек микросервисов)
curl -X POST http://localhost:8081/api/reservations \
  -H "Content-Type: application/json" \
  -d '{"clientId":1,"tableId":1,"reservationDateTime":"2024-12-25T20:00:00","numberOfGuests":2}'
```

## 📊 Метрики и мониторинг

### Prometheus запросы
```
http_server_requests_seconds_count
jvm_memory_used_bytes
rabbitmq_connections
```

### Grafana дашборды
- HTTP Requests: `rate(http_server_requests_seconds_count[5m])`
- JVM Memory: `jvm_memory_used_bytes`
- Response Time: `http_server_requests_seconds{quantile="0.95"}`

## 🔄 Jenkins Pipeline

1. Открыть http://localhost:8088
2. Создать Pipeline: `restaurant-microservices`
3. Настроить Git repository
4. Script Path: `Jenkinsfile`
5. Build Now → Console Output

## ✅ Финальная проверка

```bash
# Статус всех контейнеров
docker compose ps

# Логи после создания бронирования
docker compose logs --tail 10

# Проверка очередей RabbitMQ (через веб-интерфейс)
# Проверка метрик в Prometheus/Grafana
```

## 📋 Подробная инструкция
Смотрите: [`ПОЛНЫЙ_ГАЙД_ТЕСТИРОВАНИЯ.md`](ПОЛНЫЙ_ГАЙД_ТЕСТИРОВАНИЯ.md)
